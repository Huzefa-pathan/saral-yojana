I want to implement ALL of the following changes, ONLY as specified, without altering unrelated files or UI behavior. Please follow the steps carefully.

PART 1 — REMOVE RSS COMPLETELY
- Remove ALL RSS code from the project (server/rssFetcher.ts, server/seedData.ts, cron jobs, rss-parser imports, etc)
- Ensure the backend no longer attempts to fetch RSS data from any source
- Remove RSS-related fields from scheme objects such as publishDate, rssSource, sourceURL

PART 2 — CLEAN UP SCHEME CARD & DETAILS UI
- Remove all dates (Published on...) from cards and scheme detail pages
- Remove helpline / Need Help sections from scheme detail page
- Scheme cards should only show:
    Category badge (left)
    Source badge (right)
    Title
    District (if any)
    Short description
    “Read Details”
- Scheme detail page should only show:
    Title
    Category + Source badge
    Full description
    Eligibility & Benefits
    Documents Required (if any)
    Apply Online button (if applyOnlineLink exists)
    Apply Offline text (if applyOfflineInfo exists)

PART 3 — UPDATE DATA SCHEMA
Convert all existing and future schemes to this standardized structure:

{
  id,
  title,
  category,
  district,
  source,
  description,
  fullDescription,
  eligibility[],
  benefits[],
  documentsRequired[],
  applyMode,
  applyOnlineLink,
  applyOfflineInfo
}

Remove unsupported fields like:
publishDate, helpText, contactPhone, feedSource, etc.

PART 4 — IMPORT SCHEMES FROM MY PDF
I have manually collected scheme data in:
"/mnt/data/c8d10ea8-a9eb-44dc-91e9-af7f2c281d5b.pdf"

From this file:
- Parse the schemes
- Create scheme entries in the new structure
- Insert them into the project’s local scheme data (JSON or DB depending on implementation)

For each scheme in the PDF:
- Convert descriptions, eligibility, benefits, application methods, etc. into the structured format above
- Use fullDescription from the PDF
- If scheme is offline only, then:
    applyMode = "offline"
    applyOnlineLink = null
- If scheme is online only:
    applyMode = "online"
    applyOfflineInfo = null
- If both:
    applyMode = "both"

PART 5 — IMPORTANT RESTRICTIONS
- Do NOT modify hero section, navbar, footer, or landing UI
- Do NOT rename directories or refactor entire codebase
- Do NOT change global styling
- Do NOT break existing routes
- Do NOT touch multilingual support yet

ONLY perform what is specified above.

The final result should be:
- No RSS dependencies
- Clean UI without dates or helpline
- Real schemes from my PDF visible
- Cards and detail pages showing correct real information
- Data stored locally inside the project in structured form
