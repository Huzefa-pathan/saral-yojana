I am building a government schemes website using Express (server), React (client), and want full RSS feed integration.
My project structure is in the ZIP I uploaded:
/mnt/data/SaralYojana.zip

Make the following changes to my existing Replit project clearly and correctly:

🚀 1. Backend – Add Full RSS Integration

Create these files in server/:

server/db.ts

Create a PostgreSQL client using pg with DATABASE_URL from Replit Secrets.

server/storagePg.ts

Create a persistent storage class with methods:

upsertScheme()

listSchemes()

getSchemeById()

existing user functions ported to Postgres

server/rssFetcher.ts

Implement a full RSS fetcher using rss-parser that:

fetches from

https://www.india.gov.in/rss/schemes.xml

https://pib.gov.in/RssFeed.aspx?ModId=6&Category=agriculture

https://www.mygov.in/feeds/schemes.xml

parses XML → JSON

applies scoring:
+5 Maharashtra
+4 any district name
+3 agriculture keywords
+2 PIB agriculture

detects district using a list of 36 Maharashtra districts with aliases

detects category (Agriculture, Women & Child, Health, Students, Housing, Tribal)

creates a unique SHA-256 ID using RSS link

upserts item into Postgres (INSERT … ON CONFLICT (link) DO UPDATE)

Use the schema:

id TEXT PRIMARY KEY
title TEXT
description TEXT
link TEXT UNIQUE
published_date TIMESTAMPTZ
source TEXT
relevance_score INTEGER
district_detected TEXT
category_detected TEXT
fetched_at TIMESTAMPTZ


Store only items with relevanceScore >= 5.

🌐 2. API Routes – Modify server/routes.ts

In registerRoutes(app):

Add Express routes under /api:

GET /api/schemes

Query params:

page

size

district

category

q (search)

Return JSON: { total, page, size, schemes }

GET /api/scheme/:id

Return full scheme details.

POST /api/fetch-now

Protected using ADMIN_API_TOKEN from Replit Secrets.

Trigger manual RSS fetch.

Auto-refresh every 2 hours

Add:

setInterval(fetchAndUpsertAllFeeds, 1000 * 60 * 60 * 2)

📦 3. Shared File

Create shared/districts.ts containing full list of 36 Maharashtra districts + aliases.

🎨 4. Frontend UI Fixes (React)

Modify only these parts:

Hero Section (Home page)

Keep hero image

Fix mobile height (no overflow)

Center search box

Ensure correct padding on small screens

Remove “Browse by District” grid

Leave district dropdown in search bar.

Mic button

Remove non-working voice search button from search component.

DO NOT touch:

Trending section

Categories

Existing UI layout

🔌 5. Frontend Data Loading

Update frontend API calls to use the new endpoints:

/api/schemes

/api/scheme/:id

Implement optional auto-refresh (poll every 5 minutes).

🔑 6. Environment Variables

Use Replit Secrets:

DATABASE_URL = <postgres url>
ADMIN_API_TOKEN = <your token>

🎯 7. Final Objective

Integrate a fully working RSS → Postgres → API → React pipeline:

RSS → Parse → Score → Detect District/Category → Upsert DB → API → UI

Keep UI exactly the same except:

remove mic button

remove district grid

fix hero on mobile

Use my uploaded project ZIP as reference:
/mnt/data/SaralYojana.zip

Apply all changes directly inside my existing Replit project.
Do NOT start a new project.